//execution context   1. memory phase  2. execution phase
/*
Every thing in JS Happens inside the execution context
Have a look in*/ {Exe_context.png}

// JS is synchronous Single Threaded language

// hoisting with "let" and "const" leads to TDZ (Temporal Dead Zone)
// because they are not initialized with undefined where as "var" can


//Js has Non blocking behaviour
//callbacks old way to handle asynchronous operations
